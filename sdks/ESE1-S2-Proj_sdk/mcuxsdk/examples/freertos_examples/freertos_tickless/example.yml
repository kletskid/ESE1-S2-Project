freertos_tickless:
  section-type: application
  contents:
    meta_path: examples/freertos_examples/freertos_tickless
    project-root-path: boards/${board}/freertos_examples/freertos_tickless/${multicore_foldername}
    document:
      name: freertos_tickless${core_id_suffix_name}
      category: freertos_examples
      brief: This document explains the freertos_tickless example. It shows the CPU
        enter at sleep mode and then it is waked up by expired time delay that using
        timer module.
      example_readme:
      - examples/freertos_examples/freertos_tickless/readme.md
      - ${board_root}/${board}/freertos_examples/freertos_tickless/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards: {}
