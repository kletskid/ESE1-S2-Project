niobe4_series_wwdt_example:
  section-type: application
  contents:
    meta_path: examples/driver_examples/wwdt/niobe4_series/
    project-root-path: boards/${board}/driver_examples/wwdt/${multicore_foldername}
    document:
      name: niobe4_series_wwdt_example${core_id_suffix_name}
      category: driver_examples/wwdt
      brief: The WDOG Example project is to demonstrate usage of the KSDK wdog driver.In
        this example,quick test is first implemented to test the wdog.And then after
        5 times of refreshing the watchdog, a timeout reset is generated.
      example_readme:
      - examples/driver_examples/wwdt/niobe4_series/readme.md
      - ${board_root}/${board}/driver_examples/wwdt/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards: {}
