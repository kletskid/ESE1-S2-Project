lpi2c_polling_b2b_slave:
  section-type: application
  contents:
    meta_path: examples/driver_examples/lpi2c/polling_b2b/slave
    project-root-path: boards/${board}/driver_examples/lpi2c/polling_b2b/slave/${multicore_foldername}
    document:
      name: lpi2c_polling_b2b_slave${core_id_suffix_name}
      category: driver_examples/lpi2c
      brief: The lpi2c_polling_b2b_slave example shows how to use lpi2c driver as
        slave to do board to board transfer with a polling master:In this example,
        one lpi2c instance as slave and another lpi2c instance on the other board
        as master. Master sends a piece of data to slave, and receive a piece of data
        from slave. This example checks if the data received from slave is correct.
      example_readme:
      - examples/driver_examples/lpi2c/polling_b2b/slave/readme.md
      - ${board_root}/${board}/driver_examples/lpi2c/polling_b2b/slave/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    frdmmcxa153: []
