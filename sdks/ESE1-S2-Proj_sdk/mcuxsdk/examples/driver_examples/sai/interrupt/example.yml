sai_interrupt:
  section-type: application
  contents:
    meta_path: examples/driver_examples/sai/interrupt
    project-root-path: boards/${board}/driver_examples/sai/interrupt/${multicore_foldername}
    document:
      name: sai_interrupt${core_id_suffix_name}
      category: driver_examples/sai
      brief: The sai_interrupt example shows how to use sai functional API to implement
        interrupt playback:In this example, one sai instance playbacks the audio data
        stored in flash/SRAM using interrupt.
      example_readme:
      - examples/driver_examples/sai/interrupt/readme.md
      - ${board_root}/${board}/driver_examples/sai/interrupt/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards: {}
