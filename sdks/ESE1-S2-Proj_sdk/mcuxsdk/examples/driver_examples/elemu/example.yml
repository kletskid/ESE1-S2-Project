elemu:
  section-type: application
  contents:
    meta_path: examples/driver_examples/elemu
    project-root-path: boards/${board}/driver_examples/elemu/${multicore_foldername}
    document:
      name: elemu${core_id_suffix_name}
      category: driver_examples/elemu
      brief: The ELEMU Example project is a demonstration program that uses the KSDK
        software to show basic communication with Security Sub-System (SSS) and usage
        of SSS services to encrypt data by AES CBC with direct use of ELEMU driver.
      example_readme:
      - examples/driver_examples/elemu/readme.md
      - ${board_root}/${board}/driver_examples/elemu/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards: {}
