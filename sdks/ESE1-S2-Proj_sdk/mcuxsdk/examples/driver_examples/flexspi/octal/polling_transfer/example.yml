flexspi_octal_polling_transfer:
  section-type: application
  contents:
    meta_path: examples/driver_examples/flexspi/octal/polling_transfer
    project-root-path: boards/${board}/driver_examples/flexspi/octal/polling_transfer/${multicore_foldername}
    document:
      name: flexspi_octal_polling_transfer${core_id_suffix_name}
      category: driver_examples/flexspi
      brief: The flexspi_octal_polling_transfer example shows how to use flexspi driver
        with polling:In this example, flexspi will send data and operate the external
        octal flash connected with FLEXSPI. Some simple flash command willbe executed,
        such as Write Enable, Erase sector, Program page.Example will first erase
        the sector and program a page into the flash, at last check if the data in
        flash is correct.
      example_readme:
      - examples/driver_examples/flexspi/octal/polling_transfer/readme.md
      - ${board_root}/${board}/driver_examples/flexspi/octal/polling_transfer/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards: {}
