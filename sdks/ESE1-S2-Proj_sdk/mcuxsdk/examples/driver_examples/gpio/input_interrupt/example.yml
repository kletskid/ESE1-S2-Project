gpio_input_interrupt:
  section-type: application
  contents:
    meta_path: examples/driver_examples/gpio/input_interrupt
    project-root-path: boards/${board}/driver_examples/gpio/input_interrupt/${multicore_foldername}
    document:
      name: gpio_input_interrupt${core_id_suffix_name}
      category: driver_examples/gpio
      brief: The GPIO Example project is a demonstration program that uses the KSDK
        software to manipulate the general-purposeoutputs.The example is supported
        by the set, clear, and toggle write-only registers for each port output data
        register. The example uses the software button to control/toggle the LED.
      example_readme:
      - examples/driver_examples/gpio/input_interrupt/readme.md
      - ${board_root}/${board}/driver_examples/gpio/input_interrupt/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    frdmmcxa153: []
