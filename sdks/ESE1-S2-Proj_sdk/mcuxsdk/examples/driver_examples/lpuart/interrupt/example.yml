lpuart_interrupt:
  section-type: application
  contents:
    meta_path: examples/driver_examples/lpuart/interrupt
    project-root-path: boards/${board}/driver_examples/lpuart/interrupt/${multicore_foldername}
    document:
      name: lpuart_interrupt${core_id_suffix_name}
      category: driver_examples/lpuart
      brief: The lpuart_functioncal_interrupt example shows how to use lpuart driver
        functionalAPI to receive data with interrupt method:In this example, one lpuart
        instance connect to PC, the board willsend back all characters that PC send
        to the board.
      example_readme:
      - examples/driver_examples/lpuart/interrupt/readme.md
      - ${board_root}/${board}/driver_examples/lpuart/interrupt/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards:
    frdmmcxa153: []
