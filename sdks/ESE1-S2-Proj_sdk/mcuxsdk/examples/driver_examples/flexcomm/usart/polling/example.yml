usart_polling:
  section-type: application
  contents:
    meta_path: examples/driver_examples/flexcomm/usart/polling
    project-root-path: boards/${board}/driver_examples/usart/polling/${multicore_foldername}
    document:
      name: usart_polling${core_id_suffix_name}
      category: driver_examples/usart
      brief: The uart_polling example shows how to use uart driver in polling way:In
        this example, one uart instance connect to PC through uart, the board will
        send back all characters that PCsend to the board.
      example_readme:
      - examples/driver_examples/flexcomm/usart/polling/readme.md
      - ${board_root}/${board}/driver_examples/usart/polling/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards: {}
