dma_channel_chain:
  section-type: application
  contents:
    meta_path: examples/driver_examples/lpc_dma/channel_chain
    project-root-path: boards/${board}/driver_examples/dma/channel_chain/${multicore_foldername}
    document:
      name: dma_channel_chain${core_id_suffix_name}
      category: driver_examples/dma
      brief: The DMA channel chain example is a simple demonstration program that
        uses the SDK software.The purpose of this example is to show how to use the
        DMA channel chain feature.
      example_readme:
      - examples/driver_examples/lpc_dma/channel_chain/readme.md
      - ${board_root}/${board}/driver_examples/dma/channel_chain/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards: {}
