dmic_dma:
  section-type: application
  contents:
    meta_path: examples/driver_examples/dmic/dmic_dma
    project-root-path: boards/${board}/driver_examples/dmic/dmic_dma/${multicore_foldername}
    document:
      name: dmic_dma${core_id_suffix_name}
      category: driver_examples/dmic
      brief: This example shows how to use DMA to transfer data from DMIC to memory.
      example_readme:
      - examples/driver_examples/dmic/dmic_dma/readme.md
      - ${board_root}/${board}/driver_examples/dmic/dmic_dma/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards: {}
