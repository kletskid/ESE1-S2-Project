mu_polling_secondary_core:
  skip_build: true
  section-type: application
  contents:
    meta_path: examples/driver_examples/mu/polling/secondary
    project-root-path: boards/${board}/driver_examples/mu/polling/${multicore_foldername}/
    document:
      name: mu_polling_secondary_core
      category: driver_examples/mu
      brief: The mu_polling example shows how to use MU driver in polling way:In this
        example:1. Core 0 send message to Core 1 in polling mode via MU module.2.
        Core 1 send message back to Core 0 in polling mode.3. Core 0 receive message
        sent from Core 1 in polling mode.
      example_readme:
      - examples/driver_examples/mu/polling/readme.md
      - ${board_root}/${board}/driver_examples/mu/polling/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards: {}
