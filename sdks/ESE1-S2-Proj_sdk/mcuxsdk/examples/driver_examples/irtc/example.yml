irtc:
  section-type: application
  contents:
    meta_path: examples/driver_examples/irtc
    project-root-path: boards/${board}/driver_examples/irtc/${multicore_foldername}
    document:
      name: irtc${core_id_suffix_name}
      category: driver_examples/irtc
      brief: The IRTC project is a simple demonstration program of the SDK IRTC driver.This
        example is a low power module that provides time keeping and calendaring functions
        and additionally providesprotection against tampering, protection against
        spurious memory/register updates and battery operation.
      example_readme:
      - examples/driver_examples/irtc/readme.md
      - ${board_root}/${board}/driver_examples/irtc/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards: {}
