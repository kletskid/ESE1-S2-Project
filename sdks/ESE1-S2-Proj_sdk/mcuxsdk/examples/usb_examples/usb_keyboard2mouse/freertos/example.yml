keyboard2mouse_freertos:
  section-type: application
  contents:
    meta_path: examples/usb_examples/usb_keyboard2mouse/freertos/.
    project-root-path: boards/${board}/usb_examples/usb_keyboard2mouse/freertos/${multicore_foldername}
    document:
      name: keyboard2mouse_freertos${core_id_suffix_name}
      category: usb_examples
      brief: This example implements the host and the device, where the one controller
        works as a host and the other controller works as a device.
      example_readme:
      - examples/usb_examples/usb_keyboard2mouse/readme.md
      - ${board_root}/${board}/usb_examples/usb_keyboard2mouse/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
  boards: {}
